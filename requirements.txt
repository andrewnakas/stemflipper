# StemFlipper MVP dependency set (staged — frontier deps land with their milestones; see HANDOFF.md)
audio-separator[cpu]>=0.44,<0.45
basic-pitch>=0.4,<0.5
tflite-runtime; sys_platform == "linux"  # basic-pitch inference runtime on the Space (CoreML covers macOS)
librosa>=0.10,<0.12
numba>=0.61
pretty_midi>=0.2.10
mido>=1.3
soundfile>=0.12
gradio>=4
spaces
