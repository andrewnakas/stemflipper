{
  "name": "stemflipper-web",
  "private": true,
  "version": "2.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --port 4173 --strictPort",
    "test": "vitest run",
    "smoke": "node scripts/web_smoke.mjs"
  },
  "dependencies": {
    "@preact/signals": "^2.0.0",
    "fflate": "^0.8.2",
    "preact": "^10.24.0"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.9.0",
    "midi-file": "^1.2.4",
    "puppeteer": "^24.0.0",
    "wait-on": "^8.0.0",
    "typescript": "^5.6.0",
    "vite": "^7.0.0",
    "vitest": "^3.0.0"
  }
}
